FIND_PACKAGE(MPI)

FIND_PATH(LIBLAS_INCLUDE_PATH liblas.h)

FIND_LIBRARY(LIBLAS_LIBRARY NAMES liblas) 

ADD_EXECUTABLE(lidar_mpi lidar_mpi.cxx)

TARGET_LINK_LIBRARIES(lidar_mpi ${MPI_LIBRARY} ${LIBLAS_LIBRARY})
INCLUDE_DIRECTORIES(${MPI_INCLUDE_PATH} ${LIBLAS_INCLUDE_PATH})